name: CytoTools Build

on: push

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
        with:
          fetch-depth: 1

      - name: Set up Python 3.8
        uses: actions/setup-python@v1
        with:
          python-version: 3.8

      - name: Install Poetry
        uses: snok/install-poetry@v1.3.1

      - name: Cache Poetry virtualenv
        uses: actions/cache@v1
        id: cache
        with:
          path: ~/.virtualenvs
          key: poetry-$
          restore-keys: |
            poetry-$

      - name: Install Dependencies using Poetry
        run: poetry install
        if: steps.cache.outputs.cache-hit != 'true'

      - name: Run pytest
        run: PYTHONPATH=src/ poetry run python -m pytest -v --cov=cytotools/ tests/

      - name: Run Coverage
        run: PYTHONPATH=cytotools/ poetry run python -m coverage report -m;

      - name: Generate XML Report
        run: PYTHONPATH=cytotools/ poetry run python -m coverage xml

